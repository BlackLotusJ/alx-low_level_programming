#include <stdio.h>
/**
 * main - Entry point
 * Description: Output all single digit base 10 numbers
 * Return: Always 0 (success)
 */
int main(void)
{
	int b10;

	for (b10 = 0 ; b10 < 10 ; b10++)
	{
		printf("%d\n", b10);
	}
	return (0);
}
